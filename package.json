{
  "name": "lufie-imalat-panosu",
  "version": "1.0.0",
  "description": "Lufie imalat sipariş durum panosu (Trendyol + Shopify)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "local": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
